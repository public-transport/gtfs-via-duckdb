name: build & upload binaries

on:
  push:
    tags:
      - '*'
jobs:
  test:
    name: run tests
    uses: './.github/workflows/test.yml'

  docker-image:
    name: build & publish Docker image
    needs: [test]
    permissions:
      packages: write
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4

    - name: log into the GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ github.token }}

    - name: set up QEMU
      uses: docker/setup-qemu-action@v3

    - name: use Docker buildx
      uses: docker/setup-buildx-action@v3

    - name: build Docker image & push to Docker Hub
      uses: docker/build-push-action@v6
      with:
        push: true
        tags: |
          ghcr.io/public-transport/gtfs-via-duckdb:latest
          ghcr.io/public-transport/gtfs-via-duckdb:5
          ghcr.io/public-transport/gtfs-via-duckdb:${{ github.ref_name }}
        platforms: linux/amd64,linux/arm64
        # https://github.com/docker/build-push-action/blob/9472e9021074a3cb3279ba431598b8836d40433f/docs/advanced/cache.md#github-cache
        # https://github.com/moby/buildkit#registry-push-image-and-cache-separately
        cache-from: type=gha
        cache-to: type=gha,mode=max,oci-mediatypes=true,compression=zstd
